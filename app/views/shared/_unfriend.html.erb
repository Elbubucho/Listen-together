<%= button_to "Unfriend",
              friendship_path(friendship),
              method: :delete,
              data: { confirm: "Are you sure you want to remove #{friend.username.capitalize} from your friends?" },
              class: "btn btn-error btn-xs" %>
